/// @description Pick a random direction (just gonna randomize an x and y speed)
direction = irandom_range(0, 360);
speed = irandom_range(6, 12);
image_alpha = .025;
image_xscale = irandom_range(.85, 1.15);
image_yscale = image_xscale;